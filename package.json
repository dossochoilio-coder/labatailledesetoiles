{
  "name": "dosco-bataille-etoiles",
  "version": "1.0.0",
  "description": "DOSCO — La Bataille des Étoiles",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prepare-dist": "mkdir -p dist && cp DOSCO_Standalone.html dist/index.html",
    "cap:sync": "npx cap sync",
    "build:android": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "vite": "^5.4.0"
  }
}
